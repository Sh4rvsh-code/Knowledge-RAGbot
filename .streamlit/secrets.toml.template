# 🆓 FREE Configuration with Google Gemini!

# Option 1: Google Gemini (FREE - RECOMMENDED!)
LLM_PROVIDER = "gemini"
GEMINI_API_KEY = "your-gemini-api-key-here"
GEMINI_MODEL = "gemini-2.0-flash-exp"

# HuggingFace API Key (for Gemma model)
HUGGINGFACE_API_KEY = "your-huggingface-api-key-here"

# Database and Storage
DATABASE_URL = "sqlite:///./data/rag.db"
EMBEDDING_MODEL = "sentence-transformers/all-MiniLM-L6-v2"

# RAG Settings
CHUNK_SIZE = 512
CHUNK_OVERLAP = 50
TOP_K_RESULTS = 5
SIMILARITY_THRESHOLD = 0.7

# -----------------------------------------------------------
# Alternative: Paid Options (Better Quality)
# -----------------------------------------------------------

# Option 2: OpenAI (Paid - ~$0.002 per query)
# LLM_PROVIDER = "openai"
# OPENAI_API_KEY = "sk-your-key-here"
# OPENAI_MODEL = "gpt-3.5-turbo"

# Option 3: Anthropic Claude (Paid)
# LLM_PROVIDER = "anthropic"
# ANTHROPIC_API_KEY = "sk-ant-your-key-here"
# ANTHROPIC_MODEL = "claude-3-sonnet-20240229"

# Option 4: Local Model (Free but slower, runs on server)
# LLM_PROVIDER = "local"
# LOCAL_MODEL_NAME = "google/flan-t5-small"
